import { createContext } from "react" ;
// react context is used to know if there is someone loged in and whos loged in
// so this function will setup acontextt for us  
export default createContext(null); // so this context store no data